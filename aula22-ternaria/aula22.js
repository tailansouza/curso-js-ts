const pomtosUsuario = 9999;

//(condiçao) ? 'valor para verdadeiro' : 'valor para falço'
const usuario = pomtosUsuario >=1000 ? 'usuario vip':'nao vip';
 console.log(usuario);

 const corUsuario ='red';
 const corPadrao= corUsuario || 'black';
 console.log(corPadrao);